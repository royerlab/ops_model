"""Shared traversal/asset computation used by the paper figures and the (internal) viewer:
catalog selection, per-cell precompute, morphometrics, fluor top-cells, PC-strip crops.
Depends only on the core stages (classifier/generator/directions)."""
